package com.scalar.dl.ledger.database;

/** A mutable database abstraction. It's just for better naming consistency. */
public interface MutableDatabase<G, S, P, D, R> extends Database<G, S, P, D, R> {}
